<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#0078d4" />
    <title>@ViewData["Title"] - SyncFactors</title>
    <script src="~/js/theme-init.js" asp-append-version="true"></script>
    <link rel="icon" type="image/svg+xml" href="~/images/favicon.svg" asp-append-version="true" />
    <link rel="icon" type="image/png" sizes="32x32" href="~/images/favicon-32.png" asp-append-version="true" />
    <link rel="apple-touch-icon" sizes="180x180" href="~/images/apple-touch-icon.png" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
@{
    var currentPage = ViewContext.RouteData.Values["page"]?.ToString() ?? string.Empty;
    var currentPageToken = currentPage
        .Trim('/')
        .Replace("/", "-", StringComparison.Ordinal)
        .ToLowerInvariant();
}
<body data-page="@currentPageToken">
    <div class="shell">
        <header class="topbar" data-topbar>
            <div class="topbar-brand">
                <div class="brand-row">
                    <a class="brand" asp-page="/Index" aria-label="SyncFactors home">
                        <img class="brand-logo brand-logo-light" src="~/images/syncfactors-logo-black.png" alt="SyncFactors" />
                        <img class="brand-logo brand-logo-dark" src="~/images/syncfactors-logo-white.png" alt="SyncFactors" />
                    </a>
                </div>
            </div>
            <div class="topbar-actions">
                <nav class="nav">
                    <a class="@(currentPage == "/Index" ? "is-active" : null)" asp-page="/Index">Dashboard</a>
                    <a class="@(currentPage.StartsWith("/Sync", StringComparison.OrdinalIgnoreCase) ? "is-active" : null)" asp-page="/Sync">Sync</a>
                    <a class="@(currentPage.StartsWith("/Preview", StringComparison.OrdinalIgnoreCase) ? "is-active" : null)" asp-page="/Preview">Manual Provision</a>
                    @if (User.IsInRole("Admin") || User.IsInRole("BreakGlassAdmin"))
                    {
                        <a class="@(currentPage.StartsWith("/Admin/Users", StringComparison.OrdinalIgnoreCase) ? "is-active" : null)" asp-page="/Admin/Users">Users</a>
                        <a class="@(currentPage.StartsWith("/Admin/DeletionQueue", StringComparison.OrdinalIgnoreCase) ? "is-active" : null)" asp-page="/Admin/DeletionQueue">Deletion Queue</a>
                        <a class="@(currentPage.StartsWith("/Admin/Configuration", StringComparison.OrdinalIgnoreCase) ? "is-active" : null)" asp-page="/Admin/Configuration">Configuration</a>
                    }
                </nav>
                @if (User.Identity?.IsAuthenticated == true)
                {
                    var role = User.IsInRole("BreakGlassAdmin")
                        ? "BreakGlassAdmin"
                        : User.IsInRole("Admin")
                            ? "Admin"
                            : User.IsInRole("Operator")
                                ? "Operator"
                                : User.IsInRole("Viewer")
                                    ? "Viewer"
                                    : "Unknown";
                    <div class="auth-actions">
                        <a class="link-button" asp-page="/Logout">Logout</a>
                        <div class="muted auth-user">
                            <span>Signed in as <strong>@User.Identity.Name</strong></span>
                            <span class="auth-role">Role: @role</span>
                        </div>
                    </div>
                }
                <button class="theme-toggle" id="theme-toggle" type="button" aria-label="Toggle dark mode" aria-pressed="false">
                    <span class="theme-toggle-option theme-toggle-option-light" aria-hidden="true">☀</span>
                    <span class="theme-toggle-track" aria-hidden="true">
                        <span class="theme-toggle-thumb"></span>
                    </span>
                    <span class="theme-toggle-option theme-toggle-option-dark" aria-hidden="true">☾</span>
                </button>
            </div>
        </header>
        <main class="content">
            @RenderBody()
        </main>
    </div>
    <div class="toast-region" aria-live="polite" aria-atomic="true" data-toast-region></div>
    <script src="~/dist/site.js" asp-append-version="true"></script>
</body>
</html>
